all:
  children: 
    managers:
      hosts:
      swarm-manager:
    workers:
      hosts:
        swarm-worker-fortune:
    load_balance:
    hosts:
      load-balance:
  vars:
  ansible_python_interpreter: "/usr/bin/python3"
