---
# tasks file for swarm-manager
- name: "Init a new swarm" 
  docker_swarm:
    state: present 

- name: "show swarm info" 
  docker_swarm_info:
  register: swarm_info

# - name: "show info"
#   debug:
#     msg: "{{ swarm_info }}"
